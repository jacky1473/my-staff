# Voice Assistant — Pure Python, No External APIs
# Speech Recognition (uses Google's free built-in engine via SpeechRecognition)
SpeechRecognition==3.10.4
# Text to Speech (pyttsx3 uses system TTS — works offline, no API needed)
pyttsx3==2.90
# PyAudio for microphone input
PyAudio==0.2.14
# Requests to talk to our Flask server
requests==2.31.0
# Rich terminal UI
rich==13.7.0
